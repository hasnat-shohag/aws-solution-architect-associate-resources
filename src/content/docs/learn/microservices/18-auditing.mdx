---
title: "Auditing"
titleEn: "Auditing"
description: "CloudTrail, AWS Config, EventBridge and change management"
theme: microservices
order: 18
pages: [34, 35]
sourceUrl: https://docs.aws.amazon.com/whitepapers/latest/implementing-microservices-on-aws/implementing-microservices-on-aws.pdf
updated: 2026-09-15
keywords:
  - term: CloudTrail
    bn: AWS API call-এর audit log
    tip: management ও data event capture; সব region চালু রাখা ভালো।
  - term: CloudTrail Lake
    bn: managed audit data lake
    tip: SQL query দিয়ে audit event খোঁজা; দীর্ঘমেয়াদি retention।
  - term: AWS Config
    bn: configuration state/change recorder
    tip: resource inventory, history ও relationship timeline।
  - term: Config Rule
    bn: compliance rule check
    tip: custom/Lambda-ভিত্তিক বা managed rule।
  - term: Conformance Pack
    bn: resource config rule bundle
    tip: multi-account governance baseline।
  - term: Amazon EventBridge
    bn: event bus
    tip: CloudTrail event, config change ও schedule অনুযায়ী automation রাউট করে।
  - term: GuardDuty
    bn: threat detection service
    tip: unusual API/network activity, credentials misuse।
  - term: Security Hub
    bn: security posture aggregation
    tip: controls, standards and cross-account findings।
---

import KeywordTable from '../../../../components/content/KeywordTable.astro';
import Attribution from '../../../../components/content/Attribution.astro';
import MarkComplete from '../../../../components/progress/MarkComplete.astro';

Microservices-এ বহু টিম, account এবং automated changes থাকে। Compliance, troubleshooting এবং responsibility চিহ্নিত করতে auditable trail রাখা জরুরি।

> **Source:** Implementing Microservices on AWS, প্রিন্ট পেজ ৩৪–৩৫ (Auditing ও resource change management)।

## What records what

- **CloudTrail** API/event record রাখে; caller identity, resource, request/response metadata, source IP, timestamp।
- **CloudTrail Lake** audit data lake; SQL query দিয়ে investigate centrally।
- **AWS Config** current state, config history, resource relationships, compliance drift।
- **Config rules** specific setting check; custom rules/Lambda evaluate।
- **EventBridge** CloudTrail event match → SNS/Lambda/Step Functions automation।
- **GuardDuty/Detective/Security Hub** security context এ ব্যবহার হয়।

```mermaid
flowchart LR
  Events[API events] --> Trail[CloudTrail]
  Trail --> Lake[CloudTrail Lake]
  Config[AWS Config] --> Rules[Config Rules]
  Config --> State[State History]
  Trail --> EB[EventBridge]
  EB --> Auto[Lambda / Step Functions]
```

> [!NOTE]
CloudTrail বলে "who did what"; Config বলে "what changed and how it looks now"। প্রশ্নে difference দেখাতে জানতে হবে।

<KeywordTable keywords={frontmatter.keywords} />

<Attribution sourceUrl={frontmatter.sourceUrl} updated={frontmatter.updated} />
<MarkComplete lessonId="microservices-18" />
