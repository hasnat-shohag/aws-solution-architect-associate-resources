---
title: "Lambda অ্যাপ্লিকেশন ডিপ্লয়"
titleEn: "Deploying Lambda-Based Applications"
description: "Zip, container image, SAM/CDK, Layers, SnapStart, Powertools"
theme: microservices
order: 6
pages: [8, 8]
sourceUrl: https://docs.aws.amazon.com/whitepapers/latest/implementing-microservices-on-aws/implementing-microservices-on-aws.pdf
updated: 2026-09-15
keywords:
  - term: SAM
    bn: AWS serverless application template ও CLI
    tip: Lambda, API, DynamoDB বা auth সহ starter code দ্রুত তৈরি করতে পারে।
  - term: SAM CLI
    bn: SAM-এর local test ও deploy command-line টুল
    tip: sam local invoke ও sam deploy প্রচলিত কমান্ড।
  - term: CDK
    bn: programming language দিয়ে infrastructure as code
    tip: জটিল infrastructure-এ reusable construct ও type-safe ভাষার সুবিধা।
  - term: CloudFormation
    bn: AWS resource deployment template engine
    tip: SAM template compile হয়ে CloudFormation template-এ পরিণত হয়।
  - term: Lambda Layers
    bn: shared dependency package
    tip: একই dependency বহু function-এ ব্যবহার করার জন্য।
  - term: SnapStart
    bn: JVM cold start কমানোর ফিচার
    tip: Java 11 বা তার নতুন runtime-এ কাজ করে।
  - term: Java Optimization
    bn: Lambda Java runtime দ্রুত করার কৌশল
    tip: SnapStart snapshot ও JVM tuning একসাথে ব্যবহার করা হয়।
  - term: Powertools
    bn: Lambda utility library
    tip: logging, tracing, metrics ও idempotency দেয়।
sources:
  - label: AWS Whitepaper
    href: https://docs.aws.amazon.com/whitepapers/latest/implementing-microservices-on-aws/implementing-microservices-on-aws.pdf
    note: স্টাডি-নোট, প্রিন্ট পেজ ৮।
---

import KeywordTable from '../../../../components/content/KeywordTable.astro';
import Attribution from '../../../../components/content/Attribution.astro';
import MarkComplete from '../../../../components/progress/MarkComplete.astro';

> **Source:** Implementing Microservices on AWS, প্রিন্ট পেজ ৮ (Deploying Lambda-based applications)।

এই লেসনে Lambda code deploy করার পদ্ধতি জানা যায়। Zip সাধারণ dependency-র জন্য ভালো; container image বড় dependency বা Docker-native workflow-র জন্য ভালো।

## Deployment tools

- **AWS SAM** serverless app তৈরি ও deploy করার দ্রুত পদ্ধতি।
- **AWS CDK** programming language দিয়ে infrastructure define করে; জটিল project এ reusable construct ভালো হয়।
- **Lambda Layers** common dependencies আলাদা করে কমপ্লেক্সিটি কমায়।
- **SnapStart** Java function cold start কমায়।
- **Powertools** observability, idempotency, structured logging ও safe fault handling দেয়।

```mermaid
flowchart LR
  A[Code] --> B{Package format}
  B -->|Zip| C[zip archive]
  B -->|Container| D[container image]
  C --> E[Lambda]
  D --> E
  E --> F[CloudWatch Logs]
```

> [!NOTE]
SAM এবং CDK একই জিনিস নয়। SAM হলো Lambda-focused serverless development workflow; CDK হলো programming language-ভিত্তিক general infrastructure টুল।

<KeywordTable keywords={frontmatter.keywords} />

<Attribution sourceUrl={frontmatter.sourceUrl} updated={frontmatter.updated} />
<MarkComplete lessonId="microservices-6" />
