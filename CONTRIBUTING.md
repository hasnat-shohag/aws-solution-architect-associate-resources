# কনট্রিবিউশন গাইড

CCAR-F বাংলা স্টাডি গাইডে অবদান রাখার জন্য ধন্যবাদ। সবচেয়ে দরকারি অবদান দুটি — **অনুবাদ সংশোধন** আর **নতুন লেসনের অনুবাদ**।

## শুরু করার আগে

```bash
pnpm install
pnpm dev        # http://localhost:4321/CCAR-F/
pnpm check      # টাইপ + কনটেন্ট স্কিমা
pnpm build      # প্রোডাকশন বিল্ড
pnpm check:links    # ভেতরের লিংক যাচাই
pnpm check:contrast # রঙের কনট্রাস্ট যাচাই (হেডলেস Chrome দরকার)
```

Node 22+, pnpm 11+ দরকার। `main` থেকে নতুন ব্রাঞ্চ কেটে কাজ করুন।

## কী কী পরিবর্তন গ্রহণ করা হয়

- অনুবাদের ভুল, বানান, ব্যাকরণ, দুর্বল বাক্য
- কোড-উদাহরণ বা প্রযুক্তিগত তথ্যের ভুল (মূল Anthropic ডকুমেন্টেশনের সূত্র দিলে ভালো)
- অগ্রগতি ট্র্যাকার, থিম বা অ্যাক্সেসিবিলিটি সংক্রান্ত বাগ
- নতুন লেসনের অনুবাদ (নিচের চেকলিস্ট ধরে)

## লেসন অনুবাদের নিয়ম

1. ফাইল: `src/content/docs/learn/<domain-slug>/<task-slug>.mdx` — স্লাগ মূল সাইটের মতোই।
2. ফ্রন্টম্যাটার পূরণ করুন: `title` (বাংলা), `titleEn`, `domain`, `task`, `order`, `sourceUrl`, `updated`, `keywords`, `sources`।
3. শিরোনামের ক্রম ধরে রাখুন: এই লেসনে যা জানতে হবে → মূল অংশ → ডায়াগ্রাম (Mermaid) → পরীক্ষার ফাঁদ → প্র্যাকটিস সিনারিও → বিল্ড এক্সারসাইজ → সূত্র।
4. পাতার শেষে অবশ্যই থাকবে `<Attribution sourceUrl={frontmatter.sourceUrl} />` এবং `<MarkComplete lessonId="<domain>-<task>" />`।
5. **শব্দ ইংরেজিতেই রাখুন** — `stop_reason`, `tool_use`, `MCP`, `CLAUDE.md`, `PreToolUse`, `fork_session`, কমান্ড ও কোড। এগুলো transliterate করবেন না।
6. নতুন পরিভাষার জন্য আগে `src/content/glossary-terms.json`-এ এন্ট্রি দিন, তারপর লেসনে সেটাই ব্যবহার করুন। একই শব্দ দুই লেসনে দুইভাবে অনূদিত হলে গ্লোসারির রূপটাই চূড়ান্ত।
7. মেশিন-অনুবাদ হুবহু বসাবেন না — বাংলা বাক্য নিজে পড়ে ঠিক করুন। প্রাকৃতিক বাংলা > আক্ষরিক অনুবাদ।

## PR করার আগে চেকলিস্ট

- [ ] `pnpm check` পাস করে
- [ ] `pnpm build` পাস করে
- [ ] `pnpm check:links` আর `pnpm check:contrast` পাস করে
- [ ] ভেতরের লিংক ভাঙেনি (prev/next, সাইডবার, গ্লোসারি রেফারেন্স)
- [ ] রঙ/থিম বদলালে টেক্সটের কনট্রাস্ট ৪.৫:১-এর উপরে আছে (নিছক গ্রাফিক চিহ্নের জন্য ৩:১)
- [ ] নতুন শব্দ থাকলে `glossary-terms.json`-এ যোগ করা হয়েছে
- [ ] কমিট মেসেজ পরিষ্কার (`feat: 1.2 অনুবাদ যোগ`, `fix: 1.1 কীওয়ার্ড টেবিল সংশোধন`)

## বাগ রিপোর্ট ও অনুবাদ ভুল

Issue খুলুন: https://github.com/hasnat-shohag/CCAR-F/issues — যা লিখলে কাজ সহজ হয়:

- পাতা বা লেসনের নাম
- ভুল বাক্যটি হুবহু
- কেন ভুল, আর সঠিক কী হওয়া উচিত
- মূল ইংরেজি পাতার লিংক (থাকলে)

## কনটেন্ট-মালিকের জন্য

আপনি মূল কনটেন্টের মালিক হয়ে এই অনুবাদ অপসারণ চাইলে issue খুলুন — যুক্তি ছাড়াই যোগ্য পাতাগুলো সরিয়ে নেওয়া হবে।

## লাইসেন্স

অবদানের মাধ্যমে আপনি সম্মত হচ্ছেন: কোড অংশ MIT (`LICENSE`), আর বাংলা কনটেন্ট CC BY-NC-SA 4.0 (`LICENSE-CONTENT`)-এর অধীনে থাকবে।
